name: Marp Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Marp CLI
        uses: docker://marpteam/marp-cli:v3.4.0
        with:
          files: "*.md"
          output: pptx
